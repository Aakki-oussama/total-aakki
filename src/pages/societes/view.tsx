import SocieteViewContainer from '@/features/societes/components/view/SocieteViewContainer';

/**
 * PAGE: View Societe
 * Cette page est maintenant un simple passe-plat vers le container feature.
 * Cela permet de garder les routes propres.
 */
export default function SocieteViewPage() {
    return <SocieteViewContainer />;
}
